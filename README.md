# Infrastructure Challenge – Greeter Service

Overview

This project demonstrates automated deployment of a containerized Go web service to a Kubernetes cluster using GitHub Actions.

The application is written in Go, packaged using Docker, and deployed to Kubernetes through an automated CI/CD pipeline. The pipeline builds the container image and deploys the application automatically whenever code is pushed to the repository.
